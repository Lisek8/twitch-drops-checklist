import { Routes } from '@angular/router';
import { DropsListComponent } from './components/drops-list/drops-list.component';

export const routes: Routes = [
  {
    path: '',
    component: DropsListComponent
  }
];
